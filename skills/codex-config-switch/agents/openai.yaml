interface:
  display_name: "Codex Config Switch"
  short_description: "Import, switch, and sync Codex providers"
  default_prompt: "Use $codex-config-switch to import a Codex config.toml, switch providers, synchronize historical sessions to the active provider, or return to official Codex login."
policy:
  allow_implicit_invocation: true
